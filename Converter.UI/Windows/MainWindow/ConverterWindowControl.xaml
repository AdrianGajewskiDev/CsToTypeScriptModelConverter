<UserControl x:Class="Converter.UI.Windows.MainWindow.ConverterWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow">
    <Grid>
        <Grid Background="#000" >
            <Grid.RowDefinitions>
                <RowDefinition Height="0.5*" />
                <RowDefinition Height="5*" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>

            <Label Foreground="#fff" Margin="50 0" Padding="30 0" FontSize="11" Content="Paste your CS code below"/>
            <TextBox Name="MainTextBox" 
                     Height="auto"
                     TextWrapping="Wrap" AcceptsReturn="True" 
                     Grid.Row="1" Foreground="#000" FontSize="16"
                     AcceptsTab="True"/>
            <Button Grid.Row="2" Margin="50 10"  FontSize="17"
                    Width="150" Content="Convert" Click="Button_Click">
            </Button>
        </Grid>
    </Grid>
</UserControl>
